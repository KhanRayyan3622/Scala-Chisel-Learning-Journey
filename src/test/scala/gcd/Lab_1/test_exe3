package Lab_1

import chisel3._
import chisel3.tester._
import org.scalatest.FreeSpec
import chisel3.experimental.BundleLiterals._

class Typecast_Tester extends FreeSpec with ChiselScalatestTester
{
    "Typecast Class" in {
        test(new Typecast(8, 5)) { a =>
            a.clock.step(10)
            //a.io.result.expect(true.B)

        }
    }
}

package Lab_1

import chisel3._
import chisel3.tester._
import org.scalatest.FreeSpec
import chisel3.experimental.BundleLiterals._

class Counter2_Tester extends FreeSpec with ChiselScalatestTester
{
    "counterMSB Class" in {
        // test(new counterMSB(4.U)) { a =>
        test(new counterMSB(4)) { a =>

            a.clock.step(14)
            //a.io.result.expect(true.B)
            

        }
    }
}
